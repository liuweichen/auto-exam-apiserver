## Spring DATASOURCE (DataSourceAutoConfiguration & DataSourceProperties)
spring.datasource.url=jdbc:postgresql://localhost:5432/navigationdb
spring.datasource.username=weichen
spring.datasource.password=seaman

# The SQL dialect makes Hibernate generate better SQL for the chosen database
spring.jpa.properties.hibernate.dialect = org.hibernate.dialect.PostgreSQLDialect

# Hibernate ddl auto (create, create-drop, validate, update)
spring.jpa.hibernate.ddl-auto = update

server.ssl.key-store-name=autoexam.jks
server.ssl.key-store=classpath:${server.ssl.key-store-name}
server.ssl.key-alias=autoexam
server.ssl.key-store-password=seaman
server.port=9443